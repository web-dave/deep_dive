import { DateFDirective } from './date-f.directive';

describe('DateFDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFDirective();
    expect(directive).toBeTruthy();
  });
});
