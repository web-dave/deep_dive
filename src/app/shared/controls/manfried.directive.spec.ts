import { ManfriedDirective } from './manfried.directive';

describe('ManfriedDirective', () => {
  it('should create an instance', () => {
    const directive = new ManfriedDirective();
    expect(directive).toBeTruthy();
  });
});
